[project]
name = "anagtosuram"
version = "1.0.0"
requires-python = ">=3.14"
dependencies = [
    "requests>=2.32.3",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/anagtosuram"]
